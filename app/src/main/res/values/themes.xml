<?xml version="1.0" encoding="utf-8"?>
<!-- Launch theme only. It paints the window before Compose renders; the real
     app colours are defined in Kotlin (ui/theme/Theme.kt). -->
<resources>
    <style name="Theme.CarCareo" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
