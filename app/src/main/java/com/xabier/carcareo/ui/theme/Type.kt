package com.xabier.carcareo.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 type scale. Kept as its own file so F7 polish can tweak it
// in one place without touching Theme.kt.
val AppTypography = Typography()
